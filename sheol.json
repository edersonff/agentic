{
  "schema": 1,
  "name": "agentic",
  "description": "runs a task to completion with an LLM and tools, live TUI showing turns and tool calls, security gates, interruptible state",
  "input": "a task string in a text file as the first argument",
  "output": "the task result as json on stdout",
  "kind": "module",
  "tags": [
    "agent",
    "llm",
    "tools",
    "automation",
    "rust"
  ],
  "license": "MIT",
  "requires": [
    "rust>=1.74",
    "ripgrep"
  ],
  "needs": "llm api keys in config.yaml (copy llm-adapter's config.yaml.template and fill one provider)",
  "entry": {
    "cli": "bash try.sh input.txt"
  },
  "cost": {
    "model": "paid",
    "detail": "cost is whatever the configured llm-adapter provider charges, this module adds nothing"
  },
  "example": "test-task.txt"
}
